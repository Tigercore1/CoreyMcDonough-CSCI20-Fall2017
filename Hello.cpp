#include <iostream>
#include <fstream>
#include <cstdlib>
#include <string>
#include <vector>
#include <iomanip>
using namespace std;




int main(){
    
    string test;
    string copy;
    cin >> test;
    copy = test;
    cout << copy;
    




    
}
































/*class Shape {
   private:
      float width_;
   public:
      void setWidth(float width){ width_ = width; }
      float getWidth() { return width_; }
      float area() { return width_;}
};

class Rectangle : public Shape {
   private:
      float height_;
   public:
      void setLength(float height,float width) { height_ = height; setWidth(width); }
      float getHeight() { return height_; }
      float area() { return height_ * getWidth(); }
      float perimeter() { return height_*2 + getWidth()*2; }
};

class Circle : public Shape {
    public:
       float area() { return 3.14 * getWidth()*getWidth(); }
       float circumference() { return 2 * 3.14 * getWidth(); }
};

int main()
{
    Shape s;
    Rectangle r;
    Circle c;
    
    s.setWidth(10);
    r.setLength(10, 2);
    c.setWidth(10);
    
    cout<<"Shape area: "<<s.area()<<endl;
    cout<<"Rectangle area: "<<r.area()<<endl;
    cout<<"Circle area: "<<c.area()<<endl;
    
    cout<<"Rectangle perimeter: "<<r.perimeter()<<endl;
    cout<<"Circle circumference: "<<c.circumference()<<endl;
    
}*/

//Mark with //* 2-4 items that are important
//Mark with //? 2-4 items that are incorrect, unclear or you have questions about.
 
 /*
 Function comments:
 description
 input
 output
 
 
 style guide:
 
 function name: NormalCase
 variable name: cool_beans
 Struct name: NormalCase
 variables in a class: head_weight_ (additional underscore)
 
 Notes:
 A variable is a location in memory that stores a value.
 A parameter is a variable that a function is expecting. (function input)
 An argument is the actual value being sent to the function.
 A condition is anything that evaluates true or false
 

Procedural
Functional
Object Oriented
Event
Logical
Multi

C++ is all but logical

parts of fstream:
#include
declare objects
open
use
close

Encapsulation: objects. Creating things that are complete and can't be messed with.
Polymorphism: one command can be used in different ways in different situations. Ex: clean yourself. clean your car. clean your room. We always say "clean", but its different.
Inheritence: Taking on the traits of something else.



*/

 